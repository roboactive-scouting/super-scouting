node_modules/
dist/
.turbo/
coverage/
.vercel/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store

# database dumps — they contain every user row and never belong in the repository.
# Deliberately NOT *.sql: packages/db/supabase/migrations/*.sql must be committed.
*.backup.sql

# generated by apps/server/scripts/build-function.mjs — Vercel's deployable
# function bundle. apps/server/api/index.ts was deleted; this is the only thing
# that lives at that path now, and it is never hand-authored.
apps/server/api/*.js
apps/server/api/*.js.map
